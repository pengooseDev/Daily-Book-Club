시간없으니 필요한것만 추출


원안에 들어갈때 사용
sqrtf(x * x + y * y)


선그리기

브레젠험 알고리즘(팔분면 알고리즘)
//
1. 시작점과 끝점이 화면 영역을 벗어나는 경우 클리핑 알고리즘을 수행.
2. 시작점을 스크린좌표계로 변환하고 변수 startPos에 저장
3. 끝점을 스크린좌표계로 변환하고 변수 endPos에 저장
4. 넓이와 높이의 절댓값을 구한후 이를 비교해 완만한 경사인지 급격한 경사인지 파악후 isGradualSlope
5. isGradualSlope값에 따라 로직분류, 참일시 1458 팔분면의선 그리기진행


내적
시야각을 절반 나눈각의 cos(b) / 2
시야각B 절반나누면 왼쪽절반, 오른쪽절반



단위벡터를 구한후 내적을 계산하는 방식이  atan2를 호출하는 것보다 더 효율적이다.


vector f = unit;
vector v = (targetpos - playerPos).nornalize;
float halfFovCos = cos(mathf.deg2rad(fovangle * 0.5f));

if(v.dot(f) >= halfFovCos)
{
//in
}


외적
외적의성질을 활용하면 시선 방향을 기준으로 물체가 왼쪽, 오른쪽있는지 파악이 가능하지만
외적의결과는 벡터이므로 참거짓이 가능하도록 스칼라로 변환해야됨


방향판별
내적 => 앞,뒤
외적 => 왼,오른



백페이스 컬링


동차좌표계의 동차는 모든 항의 차수가 같음을 의미